import React from 'react';
import '../styles/Loading.css';

const Loading= props => {

    return (
        <div className="Loading">
            <span>LOADING!</span>
            <span></span>
        </div>
    )
}

export default Loading